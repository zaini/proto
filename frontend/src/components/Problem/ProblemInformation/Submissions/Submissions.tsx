import React, { useContext } from "react";
import { ProblemContext } from "../../../../views/Problem/Problem";

const Submissions = () => {
  const problem = useContext(ProblemContext);

  return <>submission results stuff</>;
};

export default Submissions;
